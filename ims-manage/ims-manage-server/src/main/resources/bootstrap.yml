server:
  port: 8010
spring:
  application:
    name: manage-Server
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
  profiles:
    active: @profiles.active@
  cache:
    type: redis

  main:
    allow-bean-definition-overriding: true


